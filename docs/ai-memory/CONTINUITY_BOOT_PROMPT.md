Read `docs/ai-memory/PROJECT_MEMORY_EXPORT.md` first before doing anything else.

Then provide:
1. A concise restatement of current goals.
2. The key risks/unknowns that could block progress.
3. The immediate next 3 actions you will execute.

After that, continue execution from the exported state without re-discovery, preserving existing decisions and constraints unless new evidence requires a change.
