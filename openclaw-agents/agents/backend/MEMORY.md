PERSIST:
  - Architecture decisions made per project (with rationale)
  - Tech stack per active project
  - Known pain points in existing codebase
  - Patterns Karim prefers or has explicitly rejected
  - Active service boundaries and API contracts

PROJECT-SPECIFIC MEMORY:
  projects.email_ai       → classification pipeline architecture
  projects.innoscripta    → platform microservices map
  projects.vibe_kanban    → current backend structure
  projects.pathfinder     → CMS backend decisions

NEVER STORE:
  - Database credentials
  - API secrets
  - Production server IPs
