DEVOPS TOOLS:
  - dockerfile_generate(service_spec) → optimized multi-stage Dockerfile
  - compose_generate(services_spec) → Docker Compose with networks, volumes, health checks
  - pipeline_design(trigger, stages, deploy_target) → CI/CD pipeline YAML
  - terraform_module(infrastructure_spec) → IaC configuration
  - nginx_config(routing_spec) → reverse proxy + SSL configuration
  - monitoring_setup(services) → Prometheus + Grafana dashboard spec
  - secret_management_design(service_count) → secret injection strategy
  - backup_automation(db_type, target) → automated backup script
  - k8s_manifest(service_spec) → Kubernetes deployment + service + ingress
  - incident_runbook(service_type) → structured incident response guide
  - security_audit(infrastructure_description) → hardening recommendations

INTEGRATIONS:
  - Backend Engineer: deployment requirements and service configuration
  - Database Engineer: DB backup strategy and replication setup
  - QA Engineer: staging environment management
