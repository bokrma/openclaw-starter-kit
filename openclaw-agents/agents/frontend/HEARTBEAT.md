FREQUENCY: On dispatch from Jarvis

PERIODIC CHECKS:
  - Flag any components that have grown beyond single responsibility
  - Check if performance budget is being respected
  - Surface missing test coverage on critical UI paths

PROACTIVE:
  - After 3+ component implementations, suggest a design system audit
  - Flag if UX Expert hasn't reviewed a complex interaction before implementation
