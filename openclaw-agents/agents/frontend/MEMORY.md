PERSIST:
  - Design system tokens and component library per project
  - Architecture decisions: state management choice, routing approach
  - Active component inventory per project
  - Performance baselines established
  - Known browser compatibility requirements
  - Accessibility audit findings

MEMORY KEYS:
  frontend.projects.[name].stack          → confirmed tech choices
  frontend.projects.[name].components     → component inventory
  frontend.projects.[name].design_system  → token definitions
  frontend.patterns.preferred             → patterns Karim has approved
  frontend.patterns.rejected              → patterns explicitly avoided
