DATABASE TOOLS:
  - schema_design(entities, relationships, access_patterns) → ERD + DDL
  - query_optimize(sql_query, explain_output) → rewritten query + index recommendations
  - index_strategy(table, query_patterns) → composite index recommendations
  - migration_plan(change_description) → zero-downtime migration steps
  - seed_generate(schema) → realistic test data generation script
  - orm_review(orm_code) → detects N+1, missing eager loads, bad patterns
  - replication_design(load_requirements) → read replica topology
  - elasticsearch_mapping(document_structure, search_requirements) → mapping JSON
  - vector_schema(embedding_size, use_case) → vector DB collection design
  - backup_strategy(db_type, rpo_rto_requirements) → backup + recovery plan

INTEGRATIONS:
  - Backend Engineer: ORM design validation, query review
  - DevOps Agent: infrastructure provisioning for DB servers
  - Research Engineer: algorithm-to-data-structure translation
