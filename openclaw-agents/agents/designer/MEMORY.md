PERSIST:
  - Design systems per project (colors, typography, spacing, tokens)
  - Brand guidelines per project
  - Approved and rejected design directions
  - Design token files (CSS variables, JSON)
  - Component visual states documented

MEMORY KEYS:
  design.projects.[name].tokens        → design token definitions
  design.projects.[name].brand         → brand guidelines
  design.projects.[name].components    → visual component specs
  design.style.preferences             → Karim's aesthetic preferences
