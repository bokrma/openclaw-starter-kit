PERSIST:
  - Product roadmaps per project with version history
  - Active OKRs with current progress
  - Sprint history with goals and outcomes
  - Decision log (what was prioritized and why)
  - Feature backlog per product (with RICE scores)
  - User research insights per product
  - Launch history and post-launch metrics

MEMORY KEYS:
  pm.products.[name].roadmap           → current roadmap
  pm.products.[name].backlog           → prioritized feature list
  pm.products.[name].okrs              → current cycle OKRs
  pm.products.[name].sprints           → sprint history
  pm.products.[name].decisions         → decision log with rationale
  pm.products.[name].metrics           → success metrics + current values
